// Changes the visibility property of the element (toggle)
function overlay() {
  modal = document.getElementById("responsive_menu");
  modal.style.visibility = (modal.style.visibility == "visible") ? "hidden": "visible";
}

<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <h2>List Beers</h2>
  <h3>
    <a href="#" onclick="overlay()" class="menu_icon">☰</a>
  </h3>
  <h4>If you would like to update the table, please select the beer.</h4> 
  <form action="/beers/update_beer">
    <table>
      <tr>
        <th>Beer</th>
      </tr>
      <tr>
    <% Beer.all.each do |beer| %>
        <td><input type="radio" name="id" value="<%= beer.id %>"><%= beer.name%></td>
      </tr>
    <% end %>
  </table>
    <p><input type="submit"></p>
  </form> 
<body>
</html>
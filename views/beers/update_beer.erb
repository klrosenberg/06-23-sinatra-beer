<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <h2>Update Beer</h2>
  <h3>
    <a href="#" onclick="overlay()" class="menu_icon">☰</a>
  </h3>
  <h4>Please change the name and submit.</h4>
  <form action="/update_beer/save">
    <input type="hidden" name="id" value="<%= @beer.id %>">
    <p><input type="text" placeholder="Beer" name="name" value="<%= @beer.name %>"></p>
    <p><input type="text" placeholder="ABV" name="abv" value="<%= @beer.abv %>"></p>
    <p><select name="beer_type_id">>
      <% BeerType.all.each do |type| %>
      <option value="<%= type.id %>" <%="selected" if type.id == @beer.beer_type_id%>><%= type.name %></option>
      <% end %>
    </select></p>
    <p><select name="brewery_id">>
      <% Brewery.all.each do |brewery| %>
      <option value="<%= brewery.id %>" <%="selected" if brewery.id == @beer.brewery_id%>><%= brewery.name %></option>
      <% end %>
    </select></p>
    <p><input type="submit"></p>
    <form>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <% if @error %>
  <p>Beer cannot be deleted if a user has rated it.</p>
  <% end %>
  <h2>Select the beer to be deleted.</h2> 
  <form action="/beer_deleted">
    <table style="width:25%">
      <tr>
        <th>Beer</th>
      </tr>
      <tr>
    <% Beer.all.each do |beer| %>
        <td><input type="radio" name="id" value="<%= beer.id %>"><%= beer.name%></td>
      </tr>
    <% end %>
  </table>
    <p><input type="submit"></p>
  </form> 
  <a href="/home">Main Menu</a>
<body>
</html>
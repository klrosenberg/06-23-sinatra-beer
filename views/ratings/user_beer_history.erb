<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <h2>User's History</h2>
  <h3>
    <a href="#" onclick="overlay()" class="menu_icon">☰</a>
  </h3>
  <h4>Please select the user to check history of beer ratings.</h4>
  <form action="/ratings/list_user_history">
    <p>User</p>
    <p><select name="user_id" value="">
      <option >--------</option>
      <% User.all.each do |user| %>
      <option value="<%= user.id %>"><%= user.name %></option>
      <% end %>
    </select></p>
    <input type="submit">
  </form>
</body>
<html>
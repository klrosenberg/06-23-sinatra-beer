<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <h2><center>Select the user to be deleted.</center></h2> 
  <form action="/user_deleted"><center>
    <table style="width:25%">
      <tr>
        <th>User</th>
      </tr>
      <tr>
    <% User.all.each do |user| %>
        <td><input type="radio" name="id" value="<%= user.id %>"><%= user.name%></td>
      </tr>
    <% end %>
  </table>
    <p><center><input type="submit"></center></p>
  </center></form> 
  <center><a href="/home">Main Menu</a></center>
<body>
</html>
<!DOCTYPE html>
<html>
<head>
  <title>Beer Rating Archive</title>
</head>
<body>
  <h2>List Users</h2>
  <h3>
    <a href="#" onclick="overlay()" class="menu_icon">☰</a>
  </h3>
  <h4>If you would like to update the table, please select the user.</h4> 
  <form action="/users/update_user">
    <table style="width:25%">
      <tr>
        <th>Users</th>
      </tr>
      <tr>
    <% User.all.each do |user| %>
        <td><input type="radio" name="id" value="<%= user.id %>"><%= user.name%></td>
      </tr>
    <% end %>
  </table>
    <p><input type="submit"></p>
  </form> 
<body>
</html>